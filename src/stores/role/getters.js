
import { Cookies } from "quasar";
const getters = {

  // crypto: (state) => {
  //   const retorno = state.data.filter((e) => e.type == 'crypto')
  //   return retorno
  // },
  // classic: (state) => {
  //   const retorno = state.data.filter((e) => e.type == 'classic')
  //   return retorno

  // }
};

export default { ...getters };
